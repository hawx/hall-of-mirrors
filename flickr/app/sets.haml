!!!
%html
  %head
    %title= title @user, 'sets'
    %link{:rel => 'stylesheet', :href => '/styles.css'}

  %body
    %header
      %h1
        %a{href: '/'} flickr
        %span /
        %a{href: @user.url}= @user.data['username']
        %span /
        %a.here{href: @user.url + '/sets'} sets

    - for set in @sets
      .set
        %a{href: set.url}
          %img{src: set.photos.first.thumbnail_url}
          %h1= set.data['title']
